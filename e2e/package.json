{
  "name": "massagelab-e2e",
  "private": true,
  "description": "End-to-end suite run against the production Docker image. Deploys are gated on it.",
  "scripts": {
    "test": "playwright test",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "@types/node": "^22.0.0",
    "typescript": "~5.9.2"
  }
}
